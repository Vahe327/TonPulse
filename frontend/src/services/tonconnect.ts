export const MANIFEST_URL = `${window.location.origin}/tonconnect-manifest.json`;

export const TON_CONNECT_UI_OPTIONS = {
  manifestUrl: MANIFEST_URL,
  actionsConfiguration: {
    twaReturnUrl: "https://t.me/TonPulseAI1_bot" as `${string}://${string}`,
  },
};
